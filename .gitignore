# 操作系统生成的垃圾文件
.DS_Store
Thumbs.db
desktop.ini

# 编辑器配置
.vscode/
.idea/
*.swp

# 依赖和构建产物（本项目用不到，但先写上，以后加工具不会误提交）
node_modules/
dist/
.cache/

# 日志
*.log
npm-debug.log*

# 临时工作目录（图片处理中间产物、原图备份）
_tmp/

# 原始素材，不是网站的一部分。
# 里面是按人写的工作稿：带「要求：每一项先给出占位图」这类写给我看的备注、
# 待核实的疑问，以及第三方的姓名。网站不引用它，推上去不会让网站更好，
# 只会白白多暴露一份。真要公开就 git add -f 个人信息.md
个人信息.md
